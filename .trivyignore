# Trivy ignore file
# Document the rationale for each suppressed finding.

# CVE-2026-30242: SSRF in Plane webhook URL serializer.
# False positive: Trivy matches our backend's distribution name "Plane" +
# version 0.24.0 against a CVE tracked against the upstream project this
# code was forked from (makeplane/plane). The "fixed in 1.2.3" refers
# to that upstream project's public release version scheme, not this
# distribution's pyproject.toml version - the SSRF mitigation has been in
# place for the applicable webhook validation code path.
CVE-2026-30242
